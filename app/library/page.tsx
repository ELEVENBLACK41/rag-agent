/**
 * 修改时间：2026-09-15
 * 文件说明：VaultAgent 独立知识库管理路由。
 * edit by：Sliye
 */

import { KnowledgeLibrary } from "@/components/library/knowledge-library";

/** 渲染文件上传、持久清单与版本状态。 */
export default function LibraryPage() {
  return <KnowledgeLibrary />;
}
