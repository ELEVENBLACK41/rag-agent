#### runs.ts 是一次问答执行的核心编排文件。它把前面 search.ts 的“检索”接起来，再调用模型生成回答，同时负责：
- 创建会话和 Run；
- 固定本次问答使用的知识库快照；
- 调用向量检索；
- 调用大模型流式生成；
- 将回答增量推送给浏览器；
- 持久化消息、引用和执行事件；
- 处理失败、断线恢复和会话删除

目前主要run.ts 主要操作四张表
conversations
    └── messages
    └── runs
            └── run_events