# conversations表
为会话表，一整个会话，例如 会话：关于项目架构的讨论，一个会话可以有多个问题和回答
字段：
- id
- workspace_id （索引）
- title 会话标题
- created_at 创建时间
- deleted_at 软删除事件 为空表示未删除
# message表
展示给用户信息的表，里面保存用户问题，agent最终的回答，用户消息在创建Run时写入，助手消息只有在模型完整问答成功后在写入
字段：
- id
- conversation_id 所属会话
- run_id
- role 消息角色  例如 user / assistant
- content 消息正文
- status 消息状态
- citations 引用信息的JSON数组
- created_at 创建时间
# runs表
一次问题的执行过程
- id
- conversation_id 所属会话
- snapshot_id 本次Run固定使用的所用快照
- status 执行状态，例如 queued / running / completed / failed / cancelled
- created_at 创建时间
- completed_at 完成时间
# run_events表
- id
- run_id
- sequence 内单调递增的事件序号
- event_type 事件类型，例如 run_started / final_delta / run_completed
- step_id Workflow 或 Agent 步骤标识
- payload 事件载荷 JSON
- created_at 创建时间