<!-- 修改时间：2026-09-22 | 文件说明：D15 固定评测执行与报告迭代记录 | edit by：Sliye -->

# D15 固定评测执行与报告

2026-09-22：加入固定快照执行器、稳定证据解析、检索与问答逐题评分、人工复核入口、版本化报告和同集对比命令。检索参数可在评测 Run 内覆盖，普通问答配置不变。报告记录数据集、语料、配置和提示哈希，并保留缺失值与失败状态。

2026-09-22 实际运行：使用 `evals/classic/compose.yaml` 的独立 PostgreSQL（端口 5544）和独立 `DATA_DIR`，通过现有导入 Workflow 发布固定快照 `b808a06f-c9e6-4a0b-b22d-61adeac789e0`。快照校验确认恰好 10 份资料且文件哈希一致。浏览器验证本地聊天页可正常渲染；独立服务在导入后关闭，评测数据库及其数据卷保留。

| 口径 | 实际结果 |
| --- | --- |
| 纯检索基线报告 | `classic-20260922T081417-retrieval-all-e09e2ceb`，60/60 执行成功，向量和 rerank 均未降级 |
| 已标注检索题 | 56/56 最终 Recall@6 = 1；最终 MRR@6 = 0.9911、nDCG@6 = 0.9934；4 道无答案题不进召回分母 |
| 检索耗时 | p50 1456 ms、p95 2201 ms，本地单轮运行值 |
| 直接检索费用 | 60 题 Embedding 实报合计 $0.00000679，rerank 实报合计 $0.00063224；不含导入索引费用 |
| 首次完整问答报告 | `rescore-20260922T082554-efd6087a`，59 题完成、1 题失败；56 道正例中严格事实加引用代理通过 55 道，失败题 `x01` 把 Block ID 误答为运行时 Chunk ID |
| 首次问答费用 | 59 个已完成 Run 的模型实报合计 $0.018841494；费用覆盖 59/60，失败 Run 仍可能产生费用，因此全量总费用标为未知 |
| 回答耗时 | 已完成 Run 的 p50 18557 ms、p95 40297 ms |
| 失败复核 | 无答案题 `n04` 首次执行失败，单题重试报告 `classic-20260922T052204-both-all-0206b52b` 执行完成；保留首次失败记录，不把重试改写为首轮成功 |

单参数对比：rerank 候选上限从 12 调为 8，其余参数不变。开发集 40 题和留出集 20 题最终 Recall@6、MRR@6 均未下降；rerank 平均实报费用分别下降 33.7% 和 32.7%。开发集平均检索耗时从 1633.8 ms 升至 1706.3 ms，留出集从 1405.0 ms 升至 1590.1 ms。时延没有稳定收益，而且简单合成资料的检索已接近满分，因此暂不改生产默认值。对比报告为 `classic-20260922T081612-retrieval-development-dc6ffc28` 与 `classic-20260922T082024-retrieval-holdout-3233072c`。

回答语义任务成功率与 4 道无答案题的拒答正确率仍待人工复核，报告保持 `null`。已有 Run 可通过 `pnpm eval:review` 补录布尔结论，无需重跑模型。合成集成绩不能外推为私人资料的真实准确率。

后续拆分执行器文件后的检索单题冒烟通过；回答单题连续遇到 Gateway 连接超时与上游流提前终止，均生成失败报告且未伪装成功。先前完整基线和 `n04` 重试已证明真实回答链路可运行，但当前网络或上游服务的瞬时失败仍影响可靠性结论。
