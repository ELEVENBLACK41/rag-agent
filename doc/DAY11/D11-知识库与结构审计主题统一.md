<!-- 修改时间：2026-09-16 | 文件说明：知识库与结构审计页面主题统一的实施及验证记录 | edit by：Sliye -->

# D11 知识库与结构审计主题统一

## 本次改动

- 两页共用 WorkspacePageHeader，统一内容宽度、标题字号、图标底色和页面留白，延续聊天工作区的中性灰主题。
- 沿用 background、card、muted、border、primary、destructive 等现有语义 token，没有新增硬编码颜色或依赖。
- 知识库上传区采用柔和底色、虚线边框和主操作按钮；文件列表统一图标容器、版本与状态标签，长路径可换行，标签移至文件信息下方，避免挤压删除按钮。
- 审计页统一操作栏、报告卡片、问题卡片与空状态；覆盖数量分为四项统计，突出检查范围；保留全部检查限制和来源入口。
- 保留固定侧栏及现有上传、删除、报告读取和原文定位逻辑。桌面宽屏分栏，较窄屏幕按单栏排列上传与文件列表。

## 实际验证

- 使用已安装工具入口运行 `node node_modules/eslint/bin/eslint.js`，本次四个 TSX 文件检查通过。
- `node node_modules/typescript/bin/tsc --noEmit` 通过。
- `git diff --check` 通过，仅提示现有 Windows 换行转换。
- 在 localhost:3000 的真实知识库和审计页面检查桌面截图，确认标题、卡片和配色一致，真实文件与审计报告正常展示。
- 操作审计类型筛选，确认筛选数量与无匹配结果提示正常。

## 验证边界

- `pnpm exec eslint/tsc` 未找到命令，改用项目已安装的对应 Node 入口完成检查，未安装或升级依赖。
- 窄屏布局和深色主题采用响应式类及既有主题变量；本次未进行窄屏与深色模式的浏览器实测。
- 本次是展示层调整，未执行上传、删除、重新检查或模型调用，未运行完整生产构建。
